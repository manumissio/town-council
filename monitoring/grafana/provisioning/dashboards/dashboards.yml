apiVersion: 1

providers:
  - name: "TownCouncilDashboards"
    type: file
    disableDeletion: true
    editable: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards

