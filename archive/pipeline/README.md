Historical `pipeline/` entrypoints live here.

Why this directory exists:
- Some older `pipeline/` tools are still useful for historical context.
- They are no longer supported active operator entrypoints.

How to use it:
- Treat these files as historical reference only.
- For active runtime and operator workflows, prefer `README.md`, `ARCHITECTURE.md`, and `docs/`.
